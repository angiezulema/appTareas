import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryBlue = Color(0xFF2962FF); // Azul principal (botones, acentos)
  static const Color lightBlueBackground = Color(0xFFE3F2FD); // Fondo azul claro para tags
  static const Color lightGrey = Color(0xFFF5F5F5); // Gris claro para fondos de cards
  static const Color darkGreyText = Color(0xFF333333); // Texto gris oscuro (más oscuro que antes)
  static const Color mediumGreyText = Color(0xFF757575); // Texto gris medio
  static const Color accentBlue = Color(0xFF448AFF); // Azul de acento para tags o elementos secundarios
}